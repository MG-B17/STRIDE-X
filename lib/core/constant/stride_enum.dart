enum AppPermission {
  activityRecognition,
  location,
  notification,
}

enum PermissionStatusResult {
  granted,
  denied,
  permanentlyDenied,
  restricted,
}

enum AppMode { light, dark, system }

enum Gender { male, female, other }
