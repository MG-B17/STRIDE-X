import 'package:flutter/material.dart';

class PremissionScreen extends StatelessWidget {
  const PremissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("PremissionScreen"),),
    );
  }
}